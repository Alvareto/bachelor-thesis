@*@model CarRentalWebSite.Models.CarViewModel

@{
    ViewBag.Title = @Html.DisplayFor(model => model.CarManufacturer) + " " + @Html.DisplayFor(model => model.CarModel);
}

<h2>Details</h2>

<div class="row">
    <div class="col-xs-4 col-sm-5 col-md-6 col-lg-7">
        <img />
    </div>
    <div class="col-xs-8 col-sm-7 col-md-6 col-lg-5">
        <h4>@ViewBag.Title</h4>
        <hr />
        <dl class="dl-horizontal">

            @foreach (var carDetail in Model.Specification)
            {
                <dt>
                    @carDetail.Type
                </dt>

                <dd>
                    @carDetail.Value
                </dd>
            }
        </dl>
        <hr />

        <p>
            @Html.ActionLink("Provjeri dostupnost", "CheckAvailability", new { id = Model.Id, from = Model.FromDate, to = Model.ToDate }) |
            @Html.ActionLink("Rezerviraj", "MakeReservation", new { id = Model.Id, from = Model.FromDate, to = Model.ToDate })
            @Html.ActionLink("<", "Previous", new { id = Model.Id })
            @Html.ActionLink(">", "Next", new { id = Model.Id })
        </p>
    </div>
</div>
<div class="row">
    <div class="col-sm-12">Comments</div>
</div>*@


